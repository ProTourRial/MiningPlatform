import { PageShell } from '@/components/dashboard/page-shell';

export default function Page() {
  return <PageShell title="Rewards" description="Reward period, alokasi reward, fee, dan rekonsiliasi upstream." />;
}
