# Validation Report — MiningPlatform v0.3.0

Release Name: **Identity & Access**  
Author: Abia Nugrahanto  
Date: 2026-07-31

## Completed in the release environment

- 67 compiled JavaScript regression tests passed across build metadata, state machine, idempotency, security, Stratum protocol, mining core, hardware detection, event bus, ledger, reward foundation, upstream resilience, mining projection, and Stratum gateway.
- TypeScript checks passed for `apps/api`, `apps/web`, `packages/shared`, `packages/security`, and `scripts`.
- API type checking used an isolated schema-facing Prisma delegate bridge because the Linux Prisma engine was unavailable; application code outside generated delegates was checked normally.
- Security package tests passed for password hashing, JWT claims, TOTP, AES-GCM encryption, backup codes, device metadata, HMAC identifiers, and worker credentials.
- Prisma schema static checks found 42 models, 30 enums, and no duplicate model or enum member names.
- The duplicate `User.notifications` relation found during review was removed.
- Production dashboard hashrate references were corrected to `HashrateSnapshot.hashrate`.
- Browser refresh tokens are delivered by HttpOnly cookie and omitted from browser JSON responses by default.
- Cookie-authenticated production mutations enforce configured origin validation.
- Authentication rate limiting uses Redis in production and development-only memory fallback.
- Source/config author attribution remains assigned to Abia Nugrahanto.

## Required verification on the target environment

The following checks remain mandatory because this environment cannot download or execute the required Linux Prisma engine and native Next.js toolchain:

```bash
pnpm install --frozen-lockfile
pnpm db:generate
pnpm typecheck
pnpm test
pnpm build
```

Database verification:

```bash
MIGRATION_TEST_ACK=v030-fresh-empty-database pnpm verify:migration:v030:fresh
MIGRATION_TEST_ACK=alpha6-upgrade-copy pnpm verify:migration:v030:upgrade
```

## Not claimed by this release

- production email delivery;
- API-key authentication of requests;
- Docker/Nginx integration completion;
- PostgreSQL/Redis end-to-end identity tests;
- load, soak, stress, or chaos readiness;
- reward settlement, wallet signing, or payout;
- production approval for real funds.

## Final release artifact checks

- Full release manifest verified: 404 payload files.
- Incremental patch manifest verified: 140 payload files.
- Applying the alpha.6 patch and running the patch installer produced a tree identical to the full v0.3.0 release: 0 missing files, 0 unexpected files, and 0 content differences.
- JSON validation: 60 files valid.
- YAML validation: 6 files valid.
- Code/config attribution: 265 of 265 applicable files contain `Author: Abia Nugrahanto`.
- Package metadata: 27 of 27 `package.json` files identify Abia Nugrahanto and version `0.3.0`.
- Packaged dependency/build/cache artifacts: 0.
- Production private keys, seed phrases, or credential tokens detected: 0.
