# Validation Report — v0.2.0-alpha.6

Owner: Abia Nugrahanto  
Date: 2026-07-31

## Results in the release environment

| Check | Result |
|---|---:|
| Automated runtime tests | 56 passed |
| Single-upstream TCP gateway | Passed |
| Primary-to-backup TCP failover | Passed |
| Modified TypeScript projects | Compiled/typechecked |
| JSON files | 60 valid |
| YAML files | 6 valid |
| Prisma models | 34 |
| Prisma enums | 25 |
| TypeScript/TSX source files | 167 |
| Code/config attribution | 200/200 |
| Full release files | 343 |
| Packaged build/dependency artifacts | 0 |
| Patch overlay missing files | 0 |
| Patch overlay extra files | 0 |
| Patch overlay content differences | 0 |
| Full release manifest | Verified |
| ZIP integrity | Verified after packaging |

The runtime suite covers mining validation, Stratum protocol parsing, upstream normalization and correlation, worker credential security, hardware detection, event support, queue backpressure, VarDiff floor enforcement, single-upstream relay, and transparent local failover to a backup simulator.

## Typecheck scope

The modified packages and services were compiled with the locally available TypeScript toolchain:

- `packages/build-info`;
- `packages/shared`;
- `packages/upstream-stratum`;
- `apps/mining-worker`;
- `apps/stratum-server`.

A complete clean-checkout monorepo typecheck must still be repeated after `pnpm install --frozen-lockfile` and `pnpm db:generate` in the target environment. The archive-provided dependency tree was not treated as release evidence.

## Mandatory external checkpoints

The following require a networked environment with the correct Prisma engine and disposable PostgreSQL/Redis services:

- clean `pnpm install --frozen-lockfile`;
- `pnpm db:generate` against schema version 6;
- full `pnpm typecheck`, `pnpm test`, and `pnpm build` after client generation;
- fresh database migration;
- upgrade migration from an alpha.5 database copy;
- Redis recovery and multi-replica tests;
- Docker and Nginx verification;
- provider-specific captured fixture verification;
- load, soak, stress, and chaos tests.

## Release classification

```text
Protocol development: suitable
Internal demonstration: suitable
Staging with selected provider: pending provider fixture and integration checks
Production miner fleet: not approved
Real funds: prohibited
```
