# Validation Report — v0.3.0-alpha.7

Date: 2026-08-22
Status: all executable local engineering gates through the full Docker E2E stack are green. Managed-source/release manifests and the final exact-tree security review are mandatory pre-commit gates; exact committed-source GitHub CI remains pending.

## Mandatory gates

- frozen-lockfile install across all 28 workspaces;
- format, lint, script typecheck, workspace typecheck, unit and integration tests;
- PostgreSQL fresh migration through schema 13;
- PostgreSQL alpha.6-to-alpha.7 upgrade rehearsal with representative data;
- payout-control integration covering step-up replay, checksum/network validation, cooldown, replacement, masking, immutability, route gating, audit, and zero payout creation;
- existing accounting and reconciliation integrations;
- production build;
- Docker Compose configuration plus API, web, outbox, mining, accounting, and Nginx E2E readiness/version/disabled-wallet checks;
- static release checks plus managed-source and release-manifest verification;
- security diff review;
- GitHub Draft PR checks on the exact commit.

## Local evidence obtained

- Frozen-lockfile install passes across all 28 workspaces.
- Monorepo lint passes 27/27; script/workspace typecheck passes 42/42; test passes 42/42; production build passes 27/27 and generates 21 Next.js routes.
- Security tests pass 16/16, including retrieval and replay rejection of the matched TOTP counter.
- Blockchain-adapter tests pass 4/4 against valid and invalid Bitcoin mainnet vectors.
- Fresh schema-13 migration and alpha.6 upgrade rehearsal both pass all 13 migrations.
- Payout-control PostgreSQL integration passes without warnings, rejects an expired step-up even when the application clock is deliberately one hour behind PostgreSQL, and creates zero payout rows.
- Accounting and reconciliation integrations pass balanced-journal, immutable-posted-entry, new-entry reversal, retry idempotency, unique-allocation, transactional rollback, and exact source-decomposition invariants.
- Docker E2E builds and starts API, web, outbox, mining, and accounting images behind Nginx, applies all 13 migrations, reports PostgreSQL/Redis/API healthy, and returns HTTP 200 for readiness, version, and disabled-wallet status. Runtime configuration confirms the 0.5% platform fee and `PAYOUTS_ENABLED=false`.
- Codex Security review identified enabled-TOTP replacement, cross-flow TOTP replay, payout API-key scope gaps, API-key TOTP mutation, and application-clock-sensitive step-up expiry. The implementation now rejects factor replacement, shares one monotonic TOTP counter across authentication flows, confines authentication-factor and payout-preference mutation to interactive sessions, requires explicit payout read scopes, and uses PostgreSQL time for step-up expiry. Regression and static acceptance tests cover these boundaries; a final exact-tree rescan remains a mandatory pre-commit gate.

These local results do not by themselves authorize merge, deployment, or real funds. Promotion evidence must include verified managed-source/release manifests, the final exact-tree security report, and GitHub checks on the exact commit.

## Required runtime boundary

- `PAYOUTS_ENABLED=false`;
- default BTC route `ADDRESS_REGISTRATION`;
- no signer/private key in API or web;
- no payout executor;
- no real upstream mining or real-funds claims.
